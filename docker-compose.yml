version: '3.7'
services:
  acquire:
    build: ./acquire
    image: acquire:latest
    env_file: .env
    restart: always
    depends_on:
      - db
    networks:
      - rti_backend
  db:
    image: postgres:12-alpine
    env_file: .env
    restart: always
    expose:
      - '5432'
    volumes:
      - rti_pgdata:/var/lib/postgresql/data
    networks:
      - rti_backend
volumes:
  rti_pgdata:
networks:
  rti_backend:
